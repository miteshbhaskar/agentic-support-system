# UI Requirements — Streamlit frontend only (app.py)
# Use this in a separate virtual environment to avoid starlette version conflicts
# with the backend (FastAPI requires starlette <0.42, Streamlit 1.x requires starlette >=0.46)

streamlit==1.40.2
httpx==0.27.2